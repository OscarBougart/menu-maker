---
target: src/App.jsx + src/styles.css + src/theme.js
total_score: 16
prior_score: 16
timestamp: 2026-05-24T15-36-00Z
slug: src-app-jsx
---
## Audit Health Score

| Dimension | Score | Prior | Delta | Key Finding |
|---|---|---|---|---|
| Accessibility | 3/4 | 3/4 | 0 | P1s still open: btn-primary resting contrast, btn hover contrast, chip active contrast |
| Performance | 3/4 | 3/4 | 0 | Clean; layout improvements use CSS, no regressions |
| Theming | 3/4 | 3/4 | 0 | Shadow still invisible; dead .status CSS still present |
| Responsive | 3/4 | 3/4 | 0 | Desktop-only by design |
| Anti-Patterns | 4/4 | 4/4 | 0 | Clean pass |
| **Total** | **16/20** | **16/20** | **0** | **Good** |

## What Changed Since Last Audit

Layout pass applied: secondary block density (House Brain, Saved Menus tightened), right panel density overrides, toggle chips fill row, font section gap via CSS, btn-primary margin 10→16px, canvas rhythm improved. All positive. No audit dimensions changed.

## Open P1 Issues (unchanged)

1. **Menu-sheet shadow invisible** — `box-shadow: 0 50px 140px color-mix(in srgb, var(--black) 80%, transparent)` — `--black` → `#f7f9fc`. Fix: `box-shadow: 0 50px 140px oklch(8% 0.01 240 / 0.55);`

2. **`.btn-primary` default text contrast fails AA** — `color: var(--black)` = `#f7f9fc` on `var(--brass)` = `#3b8bbc`. Approx 4.1:1; 11px all-caps needs 4.5:1. Fix: `color: #ffffff`.

3. **`.btn` hover text contrast fails AA** — same pattern: `color: var(--black)` on `var(--brass)` hover background. Fix: `color: #ffffff`.

## Open P2 Issues (unchanged)

4. **Chip active text contrast borderline** — `.style-chip.active`, `.font-chip.active`, `.preset-chip.active` all use `color: var(--brass)` = `#3b8bbc` on dark panel ≈ 4.0:1. Fix: `color: var(--accent-bright)`.

5. **Status badge absent** — `.status` and `.status.status-error` CSS defined, no element renders them. Status state set throughout JS but success/ready states invisible. Restore `<span className="status">` in canvas-bar.

6. **Dead CSS** — `.status`, `.status.status-error`, `.style-bar`, `.font-bar`, `.style-role-label`, `.style-divider`, `.font-row`, `.font-role-label` all defined, no DOM consumers. Should be pruned.

## Open P3 Issues (unchanged)

7. `.panel-header .kicker` and `.block-title` use `--chrome` = `#6a90aa` at 9px all-caps — borderline contrast against panel. Switch to `var(--panel-text-dim)`.

8. Panels have no explicit `z-index`. Add `z-index: 1` to `.panel` for explicit stacking context relative to canvas-bar's z-index: 10.

9. No `--shadow-base` token — `--black` semantic inversion latent.

## Recommended Actions

1. `/impeccable harden` — fix P1s (shadow, button contrast, chip contrast, status badge) in one pass
2. `/impeccable optimize` — prune dead CSS
3. `/impeccable polish` — final pass
